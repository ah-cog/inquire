<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Float" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Transparent activity used for the share bottom sheet -->
    <style name="Theme.Float.BottomSheet" parent="Theme.Material3.DayNight.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/Animation.Float.BottomSheet</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>

    <style name="Animation.Float.BottomSheet" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_up</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_down</item>
    </style>

</resources>
